---
name: ema10-trend-judgment
description: 基于EMA10极值位置判断结构（上涨/下降/区间震荡），s1=末15根整体斜率+s2=末3根斜率+量能判断阶段（上行/加速/滞涨/缩量整理/转弱/下行/加速跌/转强）
---

# 结构判断 + 阶段判断（3L盯盘版）

## 结构判断

### 算法

取最近15根**收盘价**找极值位置（四分位法）。

**为什么用收盘价不是EMA10？** 收盘价反映真实价格位置，EMA10滞后2-3天。2026-05-20第4轮定稿已确定结构用收盘价（EMA10留做阶段判定反映动能速度），但代码误用EMA10直到2026-05-22润泽科技案例暴露才改正。

```python
c15 = closes[-15:]      # 最近15根收盘价
n = len(c15)  # 15
max_pos = max(range(n), key=lambda i: c15[i])
min_pos = min(range(n), key=lambda i: c15[i])
first_q = n // 4   # 3
last_q = n - 1 - n // 4  # 11

if max_pos >= last_q and min_pos <= first_q:
    structure = '📈 上涨趋势'
elif min_pos >= last_q and max_pos <= first_q:
    structure = '📉 下降趋势'
else:
    structure = '➡ 区间震荡'
```

### ⚠️ 常见坑：EMA10不能从15根K线独立计算

**错误做法：** `e10 = ema_list(closes[-15:], 10)` ← 旧版代码踩过的坑

**根因：** EMA是指数移动平均，起始值必须继承历史数据。如果只从15根K线算，第1根EMA10等于该根收盘价（因为没有前值），导致：
- 起始15天的最高价出现在第1根 → 最大值永远在头部 → 统计偏差
- 起始15天的最低价出现在最后 → 最小值永远在尾部
- 结果：**系统性高估下降趋势**（如深圳华强从底部反弹时仍误判为下降趋势）

**修复（2026-05-21）：** 全量计算EMA10后再取末15根 —— `ema_list(closes, 10)[-15:]`

`scripts/ema_utils.py` 已修复。

### 算例对比

| 场景 | 错误算法（15根独立算） | 正确算法（全量计算取末15根） |
|:----|:--------------------|:-------------------------|
| 急跌后反弹（深圳华强） | EMA10=37.77→34.60, 最大值在[0] → 下降趋势 | EMA10=31.98→34.25, 最大值在[9] → 区间震荡 |
| 整体上涨中回调（拓普集团） | EMA10方向比0.39 → 区间震荡 | EMA10=64.47→? 极值在头部/尾部 → 上涨趋势 |

### 判定规则

| 收盘价最高位置 | 收盘价最低位置 | 结论 |
|:-------------|:-------------|:-----|
| 后25%（尾部） | 前25%（头部） | 📈 上涨趋势 |
| 前25%（头部） | 后25%（尾部） | 📉 下降趋势 |
| 其他位置 | — | ➡ 区间震荡 |

### 历史修正（2026-05-22）

**错误：** `get_structure()` 代码一直用 `ema_list(closes, 10)[-15:]`（EMA10）而非收盘价。2026-05-20第4轮定稿已明确\"结构用收盘价\"，但改完阶段判定后结构判定未被同步。

**暴露案例——润泽科技（300442）：**
- 60天从88→110→91，先涨后跌
- EMA10（滞后）最高点在4天前（idx=12），最低点在开头（idx=2）
- 用EMA10判为：**上涨趋势** ❌
- 收盘价最高点在10天前（idx=10=102.46），最低点在开头（idx=0=88.31）
- 用收盘价判为：**区间震荡** ✅

**修改后验证（全部正确）：**

| 案例 | 收盘价最高位 | 收盘价最低位 | 结果 |
|:----|:-----------|:-----------|:-----|
| 半导体 | #14（尾部）| #0（头部）| 上涨趋势 ✅ |
| 润泽科技 | #10（中部）| #0（头部）| 区间震荡 ✅ |
| 白酒 | #0（头部）| #14（尾部）| 下降趋势 ✅ |

**教训：** 定稿后必须同步检查代码实现是否一致。

## 历史记录坑

**最终定稿是EMA10极值位置法，不是形态法也不是涨天数比法。**

在skills.html的历史记录中，本skill的更新描述曾写为"形态法(涨天数比70%/30%阈值)"——但这只是迭代中的中间版本（v3），最终定稿(v4/v5)是看EMA10最高最低在15日内的尾部/头部位置。

**如需在skills.html中修正：** 不要修改旧记录，而是追加一条"每日更新记录补正"，写明实际定稿方法。

### 示例

| 板块 | EMA10最高位置 | EMA10最低位置 | 结构 |
|:----|:------------|:------------|:-----|
| 半导体 | #14（尾部） | #0（头部） | 📈 上涨趋势 |
| 白酒 | #0（头部） | #14（尾部） | 📉 下降趋势 |
| 电池 | #10（中间） | #0（头部） | ➡ 区间震荡 |

## 阶段判断

用EMA10线性回归斜率判断，s1=末15根整体斜率（趋势基准），s2=末3根斜率（近期动量，更敏感）。

```python
e10_full = ema_list(closes, 10)        # 全量60天计算EMA10
e10_last = [v for v in e10_full[-15:] if v is not None]  # 取末15根
s1 = _reg_slope(e10_last)              # 末15根整体斜率（趋势基准）
s2 = _reg_slope(e10_last[-3:])         # 末3根斜率（近期动量，更敏感）
```

**⚠️ 坑：阶段判断必须用与结构判断相同的窗口（末15根），不能用全部EMA10。**

### 区间震荡 — 用关键点支撑替代15日极值算位置

**背景：** 区间震荡的阶段判定原用最近15日最高/最低价算分位。但15日内有**单根异常大阳线或长上影**时，会拉升区间顶，导致实际在区间中部的价格被误判为顶部，进而触发 `stock-action-judgment` 的"回避模板②"误判为卖出。

**实例（大族数控 301200）：**
```
15日最低176.37(5/7)  15日最高240.00(5/13单根拉升)
当前224.00
旧算法(15日极值):  (224-176)/(240-176)=74.9% → 区间顶部 ❌ 误判卖出
```

**修复方案：** `support_level`/`resistance_level` 由调用方从K线"突"关键点计算后传入，替代原始15日极值。

```python
# get_stage() 新增参数：support_level, resistance_level
# 优先使用关键点支撑/压力，回退到15日极值
lo = support_level if support_level else min(lows[-15:])
hi = resistance_level if resistance_level else max(highs[-15:])
pct = (cur - lo) / (hi - lo) * 100
if pct < 30:   → 区间底部
elif pct > 70: → 区间顶部
else:          → 区间中段
```

**大族数控修复效果：**
```
新算法(关键点支撑): 区间[220, 240] → (224-220)/(240-220)=20.0% → 区间底部 ✅
```

**支撑选择规则（2026-05-21 修正）：**
1. 找出所有当前价下方的"突"关键点，`support_price` = 突破前10日最高价
2. 从高到低排序（最靠近当前价的优先）
3. 取第一个距当前价 **≥1.5%** 的作为有效支撑
4. 所有突关键点都距当前<1.5%时，**逐档下试**，不直接跳20日最低
5. 全无有效突关键点，才回退到20日最低

**错误历史（2026-05-21）：** `generate_review_data.py` 初期实现中，最近支撑(34.34, 距当前0.2%)被过滤后直接跳到20日最低(29.58)，导致深圳华强从底部(18.6%)被误判为中段(45.8%)。正确行为是试下一档支撑(33.10)。`batch_gen_charts.py` 逻辑本就正确（已有 `deeper` 数组试下一档），但 `generate_review_data.py` 是新写代码踩了同样的坑。

### 上涨趋势 — 加速/滞涨/上行/转弱

```
s1>0, s2>0 (整体趋势向上，近期也在向上)
  ├─ ratio > 1.8 → 🚀 加速（近期斜率远超整体，快速拉升）
  ├─ ratio < 0.4 → 需结合量能区分：
  │   ├─ 量缩80%+价在EMA10上 → 🔄 缩量整理（中继蓄力，可持股/加仓）
  │   └─ 其他                 → ⚠️ 滞涨（警惕出货，减仓/离场）
  └─ 其他        → ↑ 上行（正常健康趋势）
s1>0, s2<0       → 📉 转弱（整体向上但近期已拐头）

**⚠️ 缩量滞涨 vs 中继买点区分（参考 `2026-05-21-shrink-stagnation-vs-relay.md`）：**
同样的缩量（如量比74%），在ratio≥0.4时是「中继买点」，在ratio<0.4时是「缩量滞涨」。
核心区别不是缩量本身，而是EMA10斜率——斜率正常时缩量=供应被吸收，斜率走平时缩量=无人买。
```

**⚠️ V反误判加速（C方案 2026-05-21）：** 当ratio > 1.8时，检查近6根收盘价是否有单日跌幅>3%的急跌。如有，将s1起点从15根改为急跌后重新算，避免急跌后的V型反弹被误判为加速：

```python
if ratio > 1.8 and len(closes) >= 6:
    check = closes[-6:]
    for ci in range(1, len(check)):
        if (check[ci] - check[ci-1]) / check[ci-1] < -0.03:
            n_after = len(check) - ci
            start = max(1, len(e10_last) - n_after + 1)
            if start < len(e10_last) - 2:
                s1_adj = _reg_slope(e10_last[start:])
                if s1_adj > 0:
                    ratio = s2 / s1_adj
            break
```

**实例（伟创电气 688698，2026-05-21 修正）：**
```
5/14 收盘68.88（-3.9%急跌）
原始: s1=0.522(含回调)  s2=1.249(仅反弹)  ratio=2.39→误判加速 ❌
调整: s1_adj=1.150(从急跌后算)  ratio=1.09→上行 ✅
```

### 下降趋势 — 加速跌/下行/转强

```
s1<0, s2<0 (整体趋势向下，近期也在向下)
  ├─ ratio > 1.8 → 📉 加速跌（近期跌速远超整体）
  └─ 其他        → ↓ 下行（正常下跌）
s1<0, s2>0       → 📈 转强（整体向下但近期已拐头向上）
```

### D方案：判断整理后突破（2026-05-22 终版 — 弃用乖离率阈值）

判加速(ratio>1.8)前，检查最近有无确认过程。**有确认过程→整理后突破，降为上行；无确认过程→留加速。**

**核心洞察（讨论路径）：**

```
阶段一：纯乖离率阈值(8%) → 大族数控+8.78%卡边界，按下葫芦浮起瓢
阶段二：阈值+量比<1.7 → 伟创电气量比1.75边界误伤，调阈值无尽头
阶段三：找局部低点+回调>5%+守EMA20 → 全部正确 ✅
```

**最终结论：** 加速判断不应该靠调阈值。确认过程是一个**离散属性**（有或没有），而非连续数值。有确认过程的上涨（回调>5%守住EMA20）就是健康的突破，不管乖离率多高。完全放弃乖离率/量比阈值。

**核心逻辑（与 `scripts/ema_utils.py` 代码一致）：**

从窗口末段（最近7根）往前找最近的局部低点，检查回调幅度和EMA20守护。

```python
# 20个交易日（排除最近3天拉升段），从末尾往前找
w = closes[-23:-3]                      # 20天，用收盘价
for i in range(len(w)-1, len(w)-8, -1): # 只搜最后7根
    if w[i] > w[i-1]:                   # 股价变高→前一个是局部低点
        trough = w[i-1]                 # 谷值 = 局部最低收盘价
        peak = max(w[:i-1])             # 峰值 = 谷值前最高收盘价
        pull = (peak - trough) / peak * 100
        if pull > 5:                    # 回调>5%才有确认意义
            e20 = ema_list(closes, 20)
            ti = len(closes) - 23 + i - 1
            if trough > e20[ti] * 0.98: # 守住EMA20（允差2%）
                ratio = 1.0             # 有确认→上行，降级加速
            break  # 找到有效谷值后停止
```

**为什么只搜最后7根？** 如果最近的谷值都没守住EMA20，更旧的谷值更没意义——价格早已远离支撑。限制搜索范围避免找到早期旧回调。

**12只验证全部通过：**

| 股票 | 回调% | 守EMA20 | 结果 | 说明 |
|:--|:--:|:--:|:--:|:--|
| 多氟多 | 13.0% | ✅ | → 上行 | W底+涨停突破 ✅ |
| 伟创电气 | 7.2% | ✅ | → 上行 | 底部横盘20天+涨停 ✅ |
| 大族数控 | 7.4% | ✅ | → 上行 | 整理5天后放量突破 ✅ |
| 德业股份 | 7.0% | ✅ | → 上行 | 正常回调 ✅ |
| 中芯国际 | 6.2% | ✅ | → 上行 | 5/21已回调消化 ✅ |
| 兴森科技 | 6.7% | ✅ | → 上行 | 正常回调 ✅ |
| 华工科技 | 5.9% | ✅ | → 上行 | 正常回调 ✅ |
| 奥比中光 | 窗口末段无有效回调 | — | 留加速 ⚠️ | 无确认基础 |
| 阳光电源 | 回调破位 | ❌ | 留加速 ⚠️ | 回调跌破EMA20 |
| 广生堂 | 回调仅1.2% | — | 留加速 ⚠️ | 回调不足5% |
| 拓荆科技 | 4.3% | — | 留加速 ⚠️ | 回调不足5%，一路涨 |
| 兆易创新 | 窗口末段无有效回调 | — | 留加速 ⚠️ | 无确认基础 |

**6只转上行 + 6只留加速 = 全部正确。** 之前旧算法误判的6只（多氟多/伟创电气/大族数控/德业股份/兴森科技/华工科技）全部修正。

**设计演进（完整3轮）：**
1. ❌ 量缩50%连续3天 → 太严格，W底突破未触发
2. ❌ 两步法(守位+量比<1.7) → 伟创电气量比1.75边界误伤
3. ✅ **找局部低点+回调>5%+守EMA20** → 全部正确，彻底放弃阈值

### ⚠️ 半段切分的边界敏感性

s1用15根整体斜率、s2用末3根近期斜率，近期动量更灵敏。相比原来的对半切分，避免了拐点恰好在切分边界上导致的误判。

列线图示意：
```
15根EMA10:  ──────────────┬────
                         ↑ s1=整体趋势方向
3根近期:         ────────┘
                         ↑ s2=近期动量（更敏感）
```

### ⚠️ get_stage() 结果与 buy_point_detection 可能冲突

`get_stage()` 返回的阶段与 `buy_point_detection` 扫出的买点可能互相矛盾：

- 阶段="加速"（→卖出信号）但买点扫描="突破买点"（→买入信号）
- 这是因为阶段基于**EMA10趋势窗口（15根）**，买点基于**单日K线形态**，时间尺度不同

**处理原则（在调用方 `judge_signal()` 中实现）：** 卖出 > 买入 > 持有。阶段判定优先。详见 `stock-action-judgment` skill。

## 可复用模块

`scripts/ema_utils.py` — 可在其他脚本中 import 使用，包含：

| 函数 | 说明 | 新增参数 |
|------|------|---------|
| `get_ema_arrangement(closes)` | EMA5/10/20排列 → 多头排列/空头排列/交叉 | |
| `get_structure(closes)` | EMA10极值位置 → 上涨趋势/下降趋势/区间震荡 | |
| `get_stage(closes, structure, highs, lows, support_level=None, resistance_level=None, volumes=None)` | 半段斜率比+量能→上行/加速/滞涨/缩量整理/转弱/下行/加速跌/转强/区间底部/中段/顶部 | `support_level`, `resistance_level`, `volumes` |
| `ema_list(data, period)` | 底层EMA计算 | |

**调用方式（标准化路径，多脚本复用）：**
```python
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', '..', '..',
    'trading', 'ema10-trend-judgment', 'scripts'))
from ema_utils import get_structure, get_stage

closes = [k['close'] for k in klines]
structure = get_structure(closes)
stage = get_stage(closes, structure=structure, highs=highs, lows=lows)
```

**⚠️ 坑：在任何脚本中都必须从 ema_utils 导入，不得内联自己的算法。**
已导入的脚本（2026-05-21 统计）：
- `generate_review_data.py` — 持仓股结构和阶段显示
- `batch_gen_charts.py` (`daily-3l-review/scripts/`) — 个股SVG支撑/压力线
- `monitor_data.py` — 板块盯盘结构/阶段

**⚠️ 跨脚本重导入：** 如果多次调用 `sys.path.insert` + `from ema_utils import ...` 不生效（已缓存），用 `sys.modules.pop('ema_utils', None)` 清理后再导入。

## 代码位置

- `scripts/ema_utils.py` — 可复用模块
- `monitor_data.py` → `_compute_one()` — 板块判断实现
